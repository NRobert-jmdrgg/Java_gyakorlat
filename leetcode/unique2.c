#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

bool uniqueOccurrences(int* arr, int arrSize);

int main() {
    int arr[] = {1, 2, 2, 2, 1, 1, 3};
    int arrSize = sizeof(arr) / sizeof(int);
    
    return 0;
}



bool uniqueOccurrences(int* arr, int arrSize) {
    
    return true;
}